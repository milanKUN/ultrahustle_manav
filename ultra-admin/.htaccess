<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /ultra-admin/

    # Redirect everything to public folder
    RewriteCond %{REQUEST_URI} !^/ultra-admin/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>